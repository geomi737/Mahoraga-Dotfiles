/* Базовый стиль для всех модулей */
.modules-left > *,
.modules-center > *,
.modules-right > * {
    margin: 0 4px;              /* промежутки между модулями */
    padding: 2px 6px;           /* внутренняя «подушка» */
    border: 2px solid rgb(31,61,61);
    border-radius: 6px;
}

/* Индивидуальные модификаторы */
#wireplumber.muted {
    border-color: #f53c3c;
}

#tray > .needs-attention {
    border-color: #eb4d4b;
}
